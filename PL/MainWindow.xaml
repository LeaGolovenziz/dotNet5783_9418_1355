<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" WindowStartupLocation="CenterScreen">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height ="*"/>
            <RowDefinition Height ="auto"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions >

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="*"/>
                <ColumnDefinition  Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="manager" HorizontalAlignment="Right" Height="70" Click="Button_Click" Margin="0,0,25,0" Width="270"/>
            <Button Content="buyer" Grid.Column="1" HorizontalAlignment="Left" Height="70" VerticalAlignment="Center" Width="270" Click="Button_Click_1" Margin="25,0,0,0"/>

        </Grid>

        <MediaElement x:Name="video" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" Grid.Row="2" Volume="0.05" Position="00:00:00" />
    </Grid>

</Window>
