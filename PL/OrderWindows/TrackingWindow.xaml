<Window x:Class="PL.OrderWindows.TrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.OrderWindows"
        mc:Ignorable="d"
        Title="TrackingWindow"  WindowStartupLocation="CenterScreen">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="68*"/>
            <RowDefinition Height="93*"/>
            <RowDefinition Height="273*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="OrderIDTextBox" Grid.Column="1" PreviewTextInput="IDprev" HorizontalAlignment="Left" Margin="0" TextWrapping="Wrap" VerticalAlignment="Center" Width="149" Height="24"/>
            <Label x:Name="OrderIDLable" Grid.Column="0" Content="Order ID:" HorizontalAlignment="Right" VerticalAlignment="Center" Width="80" Margin="0" Height="26"/>
            <Button x:Name="trackOrderButton" Grid.Column="2" Content="Track order" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Center" Height="26" Width="144" Click="trackOrderButton_Click"/>
        </Grid>
        <Grid x:Name="StatusGridView" Grid.Row="1" Visibility="Hidden">
            <Label x:Name="statusLabel" Grid.Row="0" Content="Order status:" HorizontalAlignment="Left" Margin="256,32,0,0" VerticalAlignment="Top" Width="132" Height="34"/>
            <TextBlock x:Name="statysTextBlock" HorizontalAlignment="Left" Margin="393,33,0,0" Grid.Row="0" TextWrapping="Wrap" Text="{Binding Path=OrderStatus, Mode=TwoWay}" VerticalAlignment="Top" Width="169" Height="34"/>
        </Grid>

        <ListView x:Name="trackingListView" Visibility="Hidden" Grid.Row="2" Margin="167,50,168,50" ItemsSource="{Binding}" FontSize="20" FontFamily="Arial" FontWeight="Bold" FontStyle="Italic">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Path=Item1, Mode=TwoWay }" Width="230"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Path=Item2, Mode=TwoWay }" Width="230"/>
                </GridView>
            </ListView.View>
        </ListView>


    </Grid>
</Window>
